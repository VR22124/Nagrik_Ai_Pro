{
  "name": "nagrikai-pro-server",
  "version": "1.0.0",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "test": "node --test tests/unit/*.test.js tests/integration/*.test.js",
    "lint": "npx eslint src --ext .js,.jsx || exit 0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "express": "^4.21.2",
    "helmet": "^7.1.0"
  },
  "devDependencies": {
    "supertest": "^7.1.1"
  }
}
